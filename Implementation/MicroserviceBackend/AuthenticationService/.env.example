# ───── Server ─────
PORT=3000
NODE_ENV=development

# ───── Database ─────
# NOTE: hostname should match your docker‑compose service name, not localhost.
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mydatabase

# ───── JWT Secrets ─────
ACCESS_SECRET=__REPLACE_WITH_RANDOM_STRING__
REFRESH_SECRET=__REPLACE_WITH_RANDOM_STRING__


